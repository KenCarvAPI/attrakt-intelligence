/**
 * Central model constant for all Claude-powered agents and synthesis.
 *
 * Change this in one place to move every agent (context synthesis, pulse
 * digests, campaign briefs, threat detection) to a new model.
 */
export const CLAUDE_MODEL = 'claude-opus-4-8';
